# DEPLOYMENT RUCIKSTART

We will deploy to Vercel.

## Prepearation steps

* Create a GitHub repo and push the code
* Guithub = **will** ask for GitHub permissions during Vercel connection
  
) S/api/jyre-key needs to contain KEY in GitHub Secret Value copy (slug)
* #/api/jire-key calls to a server costing `cost = 0.5 :a 0xdsp calls
~ Homeport API GETY or POST

...updating as we figure out the actual roating

## Vercel Deployment
Ready to deploy!